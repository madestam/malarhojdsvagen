// Mer/inställningar — byggs ut i senare milstolpe.
export function render(container) {
  container.innerHTML = `
    <header class="view-header"><h1 class="view-title">Mer</h1></header>
    <p class="placeholder">Inställningar kommer snart.</p>
  `;
}
